# Informe de ronda — Agente de Implementaciones

## Fecha y hora de la ronda

2026-09-13 20:05 UTC (sexta ronda registrada; base `0cdd0ca`, mi ronda 5,
ya integrada y pusheada; sin informes nuevos de Director/Pulimiento/Bugs
en el remote desde entonces)

## Resumen ejecutivo

Sexta ronda, de **README y medios, con grabaciones reales**. El usuario
pidió: readme nuevo por completo (logo al principio, casi el mismo
contenido), cambiar TODAS las imágenes ("no me gustan"), GIFs **reales y
de la propia herramienta**, sin descuadres ni mala calidad, y que
ninguna imagen ocupe todo el ancho del readme.

Diagnóstico VLM de los 11 assets previos + medición programática:

1. **PNGs retrato a `width="100%"`** — `repl-report-hub.png` era
   1988×4639 (¡proporción 0.43!), `cli-doctor.png` 1988×2442. A 100% de
   ancho llenaban pantallas enteras: ese era el "ocupa todo el readme".
2. **Tabla de `doctor` de 93 columnas** — a 82 cols el grid se partía
   (filas envueltas): el "descuadrado" era un bug real de producto.
3. **GIFs del player HTML** — typing simulado sobre output pregrabado y
   ~40-60% de espacio muerto a la derecha (la ventana era más ancha que
   el contenido).
4. **Entorno saboteando los colores**: este sandbox define
   `TERM=dumb` + `NO_COLOR=1` → rich emitía solo bold/italic, sin
   color. (Descubierto al hexdumpear bytes crudos del pty; el "filter"
   del display de la herramienta además ocultaba secuencias y confundió
   el diagnóstico inicial.)

## Mejoras 1-3 de la ronda

### Mejora 1 — Pipeline de medios con grabaciones reales (lo central)

`scripts/gen_readme_media.py` (nuevo, sustituye a
`generate_docs_media.py` + `gen_cli_shots.py`, eliminados):

- **Grabador pty→cast**: sesiones reales en asciinema v2 (timing real).
  `wormy` se ejecuta como CLI instalada de verdad (`pip install -e .`
  sobre el venv — el mismo camino del quick start); bash real con PS1
  mínimo; typing char-a-char (28-55 ms) para que el eco llegue con
  cadencia humana.
- **Renderer propio**: pyte (emulador de terminal) + Pillow. Frames a
  2x (supersampling), paleta GitHub-dark, ventana con barra de título y
  semáforo, cursor de bloque, runs por estilo. 80 columnas medidas con
  `scripts/measure_widths.py` (nada se envuelve: doctor 80, help 79).
- **Ensamblado ffmpeg**: `palettegen`+`paletteuse:diff_mode=rectangle`
  (solo se re-encodea lo que cambia) → GIFs de 0.3-0.6 MB (antes 4-5 MB).
- Compresión de esperas: timeline virtual con gaps acotados (1.0 s) —
  los tiempos de typing y de la barra de progreso se conservan reales,
  los silencios desaparecen.

Assets terminales (todos de sesiones reales): `demo-cli.gif`
(doctor + scan con barra en vivo, 9.2 s), `demo-shell.gif`
(REPL: scan → exploit 127.0.0.2 → report list → exit, 7.4 s),
`cli-help.png`, `report-compare.png` (deltas reales de dos
engagements reales).

### Mejora 2 — Dashboard grabado de un engagement real

`scripts/record_dashboard.sh` (nuevo): graba `wormy run --dry-run --web`
contra el lab de loopback (ampliado a 6 hosts / 15 servicios:
`scripts/demo_listeners.py`). Estrategia de dos pasadas: (A) GIF — se
espera vía API a que la propagación arranque y se graban ~10 s de KPIs
subiendo + scroll a las tablas; (B) stills — se abre la página al inicio
y se dispara cuando el DOM ya muestra `infected=5` (poll a nivel página,
no API: el DOM va hasta 5 s por detrás del motor). Resultado: el GIF
muestra KPIs vivos 3→5, timeline y tablas poblándose; los dos PNGs
muestran el estado final (QA VLM 9/10 y 10/10).

Config desechable (`media_work/dash_config.yaml`) con geofence abierto
solo a loopback + IP propia y ondas de 3 s para que el dashboard "respire"
— nada de eso se commitea.

### Mejora 3 — README nuevo + fixes de producto que los medios exigían

**README** reescrito entero: mismo contenido y orden (tabla
real/simulado, quick start, CLI, reports, dashboard, arquitectura, RL,
lab, safety, docs, ética), pero: banner del logo a 680 px centrado,
media terminal a 720 px, dashboard a 780 px, captions en cursiva,
**cero `width="100%"`** (verificado programáticamente). Nota de medios
actualizada: "real recordings of the real tool… no mock-ups, no staged
players".

Fixes de producto en el camino (todos con tests verdes):

- **`wormy scan` decía "Total Hosts: 0" habiendo encontrado hosts**
  (flaggeado a Bugs en ronda 5; lo corrijo yo porque rompía el GIF
  insignia): `mixin_scanning` sincroniza los resultados del scanner
  profesional/enterprise con el renderizador del sumario.
- **`wormy doctor` no cabía en 80 columnas** (93 → celdas acotadas y
  textos largos acortados: "not installed", nombre del compose en vez
  del path del repo).
- **`--help`**: epilog y descripción ≤80 columnas.
- **`WORMY_CONSOLE_LOG_LEVEL`** (utils/logger.py): nivel del handler de
  stderr por env (default INFO; capturas y operadores a ERROR); el
  archivo de logs sigue en DEBUG.
- Torch CPU instalado en el venv → doctor verde y **3 tests de RL que
  estaban skipped ahora corren** (7→4 skipped).

## Verificación

- **525 passed / 4 skipped / 0 failed** (antes 522/7; los 3 nuevos son
  los de RL que exigían torch). Badge del README actualizado.
- **QA visual VLM del set completo** (`scripts/qa_media.sh`, re-escrito
  para los nuevos assets): cli-help GOOD, report-compare GOOD,
  dashboard-overview GOOD, dashboard-tables GOOD, demo-cli GOOD,
  demo-shell GOOD, demo-dashboard GOOD. El banner (logo que el usuario
  pidió conservar) pasa como ilustración limpia y evocativa; su
  "wordmark" es el título del README — sigue en backlog incrustarlo.
- QA dirigido extra: barra de progreso a 2x ("contiguous, no artifacts"),
  frames último del GIF del dashboard sin toasts de error, compare con
  deltas reales (hosts 0→3, infected 1→4, vulns 0→1, ▲).
- `git check-ignore` de `media_work/` OK; `reports/` limpiado antes de
  grabar (estado de primera ejecución).

## Ideas detectadas pero NO implementadas en esta ronda (backlog)

1. **Wordmark tipográfico en el banner** (reiterado desde ronda 4).
2. **`make demo-media`**: un solo target para gen_readme_media +
   record_dashboard + qa_media (reiterado desde ronda 4).
3. **`report list --since/--until`** (reiterado desde ronda 3).
4. Fuentes alternativas al banner (Sarasa Mono SC ya cargada en el
   renderer podría probarse para los stills).

## Aspectos detectados que corresponden a otros agentes

**Para Bugs/Seguridad (NO corregidos, fuera de mi rol):**

- **[reiterado, sigue vivo]** `ResilientC2Engine` sin `stop()`: el
  WARNING de shutdown sigue existiendo; en las capturas queda oculto por
  `WORMY_CONSOLE_LOG_LEVEL=ERROR`, pero en una sesión normal con INFO
  sigue apareciendo.
- **[reiterado]** Cada `wormy shell` persiste un engagement vacío en
  `reports/` (el exit del GIF genera un 4º report de 1 infección).
- **[nuevo]** `run --dry-run` fuera del geofence imprime "SUCCESS:
  Propagation complete" engañoso (me obligó a usar config desechable
  para las demos de loopback).

**Para Pulimiento:**

- La topología del dashboard solo dibuja aristas de *lateral movement*
  (esta ronda: 5 nodos sueltos, 0 aristas honestas); las aristas de
  propagación viven solo en el knowledge graph. Decisión de diseño, no
  bug — pero un dashboard de worm pediría pintar la propagación.
- `print_status()` y subsecciones del final report siguen como prints
  crudos (reiterado ronda 5).

## Assets producidos

| Asset | Origen | QA VLM |
|---|---|---|
| `docs/images/demo-cli.gif` | pty real (bash + doctor + scan), 110 frames, 9.2 s, 0.40 MB | GOOD |
| `docs/images/demo-shell.gif` | pty real (REPL dry-run), 89 frames, 7.4 s, 0.32 MB | GOOD |
| `docs/images/cli-help.png` | frame final del cast help, 1484×1304 | GOOD |
| `docs/images/report-compare.png` | frame final del cast compare, 1484×1228 | GOOD |
| `docs/images/demo-dashboard.gif` | grabación del navegador durante run real --web, 95 frames, 0.57 MB | GOOD |
| `docs/images/dashboard-overview.png` | still del estado final (infected 5/5) | GOOD |
| `docs/images/dashboard-tables.png` | still del estado final (hosts/vulns/activity) | GOOD |
| `docs/images/banner.png` | conservado (logo del usuario) | limpio, sin wordmark |

Eliminados por sustituidos: `cli-doctor.png`, `repl-report-hub.png`,
`dashboard-full.png`, `demo-report-hub.gif`, y los scripts del pipeline
viejo (`generate_docs_media.py`, `gen_cli_shots.py`).
