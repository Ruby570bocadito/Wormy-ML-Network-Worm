# ═══════════════════════════════════════════════════════════════════
# Wormy ML Network Worm v3.0 — Requirements
# Full enterprise-grade red team framework
# ═══════════════════════════════════════════════════════════════════

# ── Core Networking ──────────────────────────────────────────────
scapy>=2.5.0
python-nmap>=0.7.1
netifaces>=0.11.0
dnspython>=2.4.0
requests>=2.31.0
urllib3>=2.0.0
msgpack>=1.0.0

# ── Machine Learning & RL ─────────────────────────────────────────
torch>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.1.0
gymnasium>=0.29.0

# ── Exploitation Engines ──────────────────────────────────────────
impacket>=0.11.0           # SMB, Kerberoasting, AS-REP Roasting, DCSync
paramiko>=3.4.0            # SSH brute force + lateral movement

# ── Database Exploitation ─────────────────────────────────────────
pymysql>=1.1.0             # MySQL exploitation & credential attacks
pymongo>=4.0.0             # MongoDB noauth exploitation & data dump
psycopg2-binary>=2.9.0     # PostgreSQL exploitation

# ── Active Directory ──────────────────────────────────────────────
ldap3>=2.9.0               # LDAP enumeration (users, groups, SPNs, AD objects)

# ── Cryptography & Obfuscation ────────────────────────────────────
cryptography>=41.0.0
pycryptodome>=3.19.0

# ── Graph & Visualization ─────────────────────────────────────────
networkx>=3.0
matplotlib>=3.7.0

# ── CLI & UI ─────────────────────────────────────────────────────
rich>=13.0.0               # Rich terminal monitor (real-time dashboard)
colorama>=0.4.6
tqdm>=4.66.0

# ── C2 Server ────────────────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
websocket-client>=1.7.0       # WebSocket C2 protocol

# ── Process & System ─────────────────────────────────────────────
psutil>=5.9.0              # Process enumeration (sandbox + EDR detection)

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.1

# ── Testing ──────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Optional / Platform-specific ─────────────────────────────────
boto3>=1.34.0              # AWS reconnaissance & exploitation
pywin32>=306               # Windows DCOM / COM lateral movement
# pyOpenSSL>=23.0.0        # For TLS C2 channels
# aiohttp>=3.9.0           # Async HTTP for web exploits
# selenium>=4.15.0         # Browser-based exploitation